# Allowlist gitignore: everything ignored by default; opt in below.
# Add a new tracked path by appending `!/<path>` (and the directory's
# parents, if any). Subdirectory contents follow normal rules once the
# directory itself is un-ignored.

/*

!/.gitignore
!/README.md
!/flake.nix
!/flake.lock
!/lib/
