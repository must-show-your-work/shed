# Secrets — never commit
.env
.env.local
.env.*.local
# Ephemeral agent-fetched tokens (e.g. balrog leader token pulled from Vault).
# Treat like .env: file is local-only, never goes in history.
.agent-tokens

# Local-only Claude settings (the shared settings.json, if any, may be tracked)
.claude/settings.local.json

# Direnv / nix dev shell caches
.direnv/
result
result-*

# Python venvs
.venv/

# Editor / OS scratch
*.swp
*~
.DS_Store

# Source-text PDFs (copyrighted textbook material — never publish).
sources/
